# Create a new array for collected student name
studentNames = ["James", "John", "Marry", "Dannie", "stephen"]

# Call all student name in array
print(studentNames)

# Call a first of student name in array
# fisrt index in array started is 0
print(studentNames[0])

# Call a second and third of student name in array
print((studentNames[1]),(studentNames[2]))

# Concat calling array with string (and)
print((studentNames[0]) + " "+ "and" + " " + (studentNames[2]))